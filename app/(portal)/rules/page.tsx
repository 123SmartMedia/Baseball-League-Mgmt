import type { Metadata } from "next";

export const metadata: Metadata = { title: "Rules" };

export default function RulesPage() {
  return (
    <div>
      <h2 className="mb-6">Rules</h2>
      {/* Filter by age group / league vs tournament */}
    </div>
  );
}
