import type { Metadata } from "next";

export const metadata: Metadata = { title: "Standings" };

export default function StandingsPage() {
  return (
    <div>
      <h2 className="mb-6">Standings</h2>
      {/* StandingsTable component */}
    </div>
  );
}
